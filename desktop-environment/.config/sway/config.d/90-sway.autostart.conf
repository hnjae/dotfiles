# sway.autostart

exec --no-startup-id foot --server
# exec_always --no-startup-id $program-relauncher kime-wayland
# exec_always --no-startup-id $program-relauncher kime-indicator

exec_always --no-startup-id "$HOME/.local/bin/__wlroots-auto-randr"
