# Open in split
`Shift` + right click

# Go to tomorrow journal
`gt`

# Go to netx/previous journal
`gn` / `gp`

# Go to journals / home
`gj` / `gh`

# toggle things
`td`: documents
`tl`: left sidebar
`tw`: wide mode
