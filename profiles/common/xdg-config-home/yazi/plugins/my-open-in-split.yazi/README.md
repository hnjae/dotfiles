# my-open-in-split.yazi

- `zv` opens the hovered entry in a vertical tmux split
- `zo` opens the hovered entry in a horizontal tmux split
- hovered directories open a new `yazi` instance
- hovered files open in `${VISUAL:-$EDITOR}`
- this currently requires running Yazi inside `tmux`
