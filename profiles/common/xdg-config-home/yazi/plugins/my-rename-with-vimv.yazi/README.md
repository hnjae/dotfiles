# my-rename-with-vimv.yazi

Local functional plugin for this dotfiles setup.

- `R` opens the selected files in `vimv`
- if nothing is selected, it falls back to the hovered entry
- runs interactively in the terminal and returns to Yazi afterward
