# my-file-create.yazi

Local functional plugin for this dotfiles setup.

- `%` creates files only
- names ending with `/` or `\\` are rejected
- `package.toml` is intentionally not used for this plugin
