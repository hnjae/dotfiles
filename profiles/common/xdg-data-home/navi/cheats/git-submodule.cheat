% git, git-submodule


# Update the URL of submodules in `.git/config` from what's in `.gitmodules`
git submodule sync --quiet --recursive

# Updates/initialize and fetch git submodules
git submodule update --init --recursive

# remove/delete git submodules
git submodule deinit --force -- <path>
