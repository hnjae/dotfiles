---
# See: https://pre-commit.com/
repos:
  - repo: local
    hooks:
      - id: stylua
        name: format lua
        entry: stylua
        language: system
        types: [lua]
      - id: selene
        name: check lua
        entry: selene --allow-warnings
        language: system
        types: [lua]
