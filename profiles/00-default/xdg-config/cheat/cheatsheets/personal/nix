---
syntax: markdown
tags: [ nix ]
---

# escape character in string

use `''`

# docker-compose to nix

use compose2nix

# generate nix package from URLs(git)

use nix-init

# find specific file in nixpkgs

nix-locate <file>

## flake

### point specific branch in flake.nix

```nix
      url = "github:nix-community/home-manager/release-25.05";
```

### point specific tag in flake.nix

```nix
      url = "github:astro/microvm.nix/refs/tags/v0.5.0";
```

## nixpkgs

### prefer local build

```nix
    (lib.customisation.overrideDerivation
      (pkgs.writeShellApplication {

      })
      (_: {
        preferLocalBuild = true;
      })
    )
```

<!--
vi:ft=markdown
-->
