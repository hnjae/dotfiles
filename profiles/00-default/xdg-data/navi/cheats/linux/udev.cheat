% udev

# Walk device attributes
udevadm info --attribute-walk  <device>

# print device info
udevadm info -a -n "/dev/<device>"

# reload & trigger
sudo udevadm control --reload-rules && sudo udevadm trigger

# Print and follow log
journalctl -f -u systemd-udevd
