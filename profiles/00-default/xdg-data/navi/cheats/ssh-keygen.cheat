% ssh-keygen, ssh

# genreate ed25519 ssh key pair
ssh-keygen -t ed25519 -C "<email>"

# genreate rsa ssh key pair (not recommended)
ssh-keygen -t rsa -b 4096 -C "<email>"

# print fingerprint
ssh-keygen -lvf <public-key-path>
