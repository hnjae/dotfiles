% badblocks

# test
badblocks -wsv /dev/<device>

# when value too large (must be 32-bit value)
badblocks -wsv -b 4096 /dev/<device>

# run first half for big drive
badblocks -wsv -b 4096 /dev/sdb 2197291008 0
