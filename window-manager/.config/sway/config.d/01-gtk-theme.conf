# vim:ft=conf
set $gnome-schema org.gnome.desktop.interface
exec_always {
    # gsettings set $gnome-schema gtk-theme 'Fluent-dark'
    # gsettings set $gnome-schema icon-theme 'Fluent-dark'
    # gsettings set $gnome-schema cursor-theme 'Fluent-dark-cursors'
    # gsettings set $gnome-schema document-font-name 'Sans 11'
    # gsettings set $gnome-schema font-name 'Sans 10'
    # gsettings set $gnome-schema monospace-font-name 'Monospace 10'
    gsettings set org.gnome.desktop.wm.preferences button-layout 'appmenu'
}
